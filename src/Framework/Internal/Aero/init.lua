local Aero = {
	Service = require(script.Service),

	ClientToServer = require(script.ClientToServer),
	ClientFunctionAsync = require(script.ClientFunctionAsync),
	ClientFunction = require(script.ClientFunction),
	ClientEvent = require(script.ClientEvent),

	Controller = require(script.Controller),

	ServerModule = require(script.ServerModule),
	ClientModule = require(script.ClientModule),
	StandaloneModule = require(script.StandaloneModule),
}

return Aero
